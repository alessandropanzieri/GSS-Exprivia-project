server {
    listen 443 ssl;
    server_name gamma-security-services.it;

    ssl_certificate /etc/nginx/ssl/certificate.crt;
    ssl_certificate_key /etc/nginx/ssl/private.key;

    location / {
        proxy_pass http://frontend:4321;
    }
}